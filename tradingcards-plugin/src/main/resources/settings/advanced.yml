# Advanced settings.
# max-cache-entries
# refresh-after-write, in minutes
cache:
  rarity:
    max-cache-entries: 100
    refresh-after-write: 5
  series:
    max-cache-entries: 100
    refresh-after-write: 5
  cards:
    max-cache-entries: 500
    refresh-after-write: 5
  types:
    max-cache-entries: 100
    refresh-after-write: 5
  packs:
    max-cache-entries: 100
    refresh-after-write: 5
  upgrades:
    max-cache-entries: 100
    refresh-after-write: 5

config-version: 1
